using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Cards.Poker_classes.Common.Player
{
    /*
    enum playStyle { maniac, looseAggressive, loosePassive, tightAggressive, tightPassive, rock }
    enum say { fold, check, bet, call, raise }

    interface IStrategy
    {
        game game { get; }
        //T playStyle { get; }
    }
    class Strategy<T>  where T: playerType, new() 
    {
        public T playStyle;
        public Strategy()
        {
            this.playStyle = new T();
        }
        public game game { get { return game.Badugi; } }
    }

    abstract class playerType
    {
        public playStyle name { get; set; }
    }
    class maniac : playerType
    {
        public int per;
        public maniac() { this.name = playStyle.maniac; }
    }


    class test
    {
        Strategy<maniac>  s = new Strategy<maniac> ();
        public test()
        {
            s.playStyle = new maniac() { per = 4 };
        }
    }
     */ 
}
